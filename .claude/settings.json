{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'source-data/'; then node /home/user/loan-jjyu/.claude/hooks/track-source-read.js log \"$(echo \"$CLAUDE_TOOL_INPUT\" | grep -o '[^/]*\\.json')\"; fi",
            "timeout": 5000
          }
        ]
      },
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(echo \"$CLAUDE_TOOL_INPUT\" | node -e \"const d=require('fs').readFileSync('/dev/stdin','utf8');try{console.log(JSON.parse(d).file_path)}catch{}\"); if [ -n \"$FILE_PATH\" ]; then node /home/user/loan-jjyu/.claude/hooks/pre-edit-verify.js \"$FILE_PATH\"; fi",
            "timeout": 5000
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(echo \"$CLAUDE_TOOL_INPUT\" | node -e \"const d=require('fs').readFileSync('/dev/stdin','utf8');try{console.log(JSON.parse(d).file_path)}catch{}\"); if [ -n \"$FILE_PATH\" ]; then node /home/user/loan-jjyu/.claude/hooks/pre-edit-verify.js \"$FILE_PATH\"; fi",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
